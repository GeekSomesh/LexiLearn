# Copy this file to `.env` and replace the placeholders with your real keys
OPENAI_API_KEY=your_openai_api_key_here
VITE_OPENROUTER_API_KEY=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

# Auth0 configuration (used by the frontend)
# Set these to your Auth0 tenant domain and client id.
VITE_AUTH0_DOMAIN=your-tenant.us.auth0.com
VITE_AUTH0_CLIENT_ID=YOUR_CLIENT_ID

# Optional: if you use an API and audience
# VITE_AUTH0_AUDIENCE=your-api-identifier

# Local API server (for chat persistence)
# If you run `npm run server`, set this so the frontend can reach it.
# VITE_API_BASE=http://localhost:8787/api

# Supabase project (frontend uses anon key for other libs; server uses service key)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY

# Server-only (do not commit real values):
# SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY

# Auth0 Management API (optional - for scripting Universal Login branding)
# Create a Machine-to-Machine app authorized for the Auth0 Management API with 'update:branding' scope.
# AUTH0_MGMT_DOMAIN is your tenant domain (same as VITE_AUTH0_DOMAIN)
# AUTH0_MGMT_CLIENT_ID=YOUR_M2M_CLIENT_ID
# AUTH0_MGMT_CLIENT_SECRET=YOUR_M2M_CLIENT_SECRET

# Suggested brand colors to match the site (pastel cream + violet)
# AUTH0_BRAND_PRIMARY=#8B5CF6
# AUTH0_BRAND_BACKGROUND=#FFFFF0
# Optional logo url
# AUTH0_BRAND_LOGO_URL=https://your.cdn/logo.png
